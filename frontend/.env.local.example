# Copy to .env.local
#
# Next inlines NEXT_PUBLIC_* into the bundle at BUILD time. Changing this needs a rebuild
# (npm run build), not just a restart — that catches people out on every hosting platform.
NEXT_PUBLIC_API_BASE=http://localhost:8000
